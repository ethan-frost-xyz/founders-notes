# Founders Notes — agent guide

Personal knowledge vault: Founders podcast transcripts, study notes, and X posts.

## Work in progress (read first)

**Incomplete notes and posts are intentional.** Phase 1 transcripts are complete; Phase 2 is an active daily ritual (~1 episode/day). Most episodes have a `.notes.md` scaffold but only **176/417** have timestamp bullets so far; posts are **187/417** and growing.

- Do **not** treat `catalog/gaps.md` “notes without datapoints” or “missing posts” as bugs to bulk-fix. Gaps **beyond the current episode** (e.g. missing posts from ep-0190 onward while ~187 posts exist) mean those episodes are **not listened to or posted yet**—not broken import.
- **Do** help with the current episode: `python scaffold_notes.py --next`, edit `{folder}.notes.md`, then `build_chunks.py`.
- Blocking gaps = transcript/layout only (`verify.py` exit 1). Empty scaffolds are normal.

Daily workflow: [`docs/notes-pipeline.md`](docs/notes-pipeline.md).

## Start here

1. **Episode index:** [`catalog/episodes.jsonl`](catalog/episodes.jsonl) — ids, URLs, `transcript_status`, paths
2. **Gaps / coverage:** [`catalog/gaps.md`](catalog/gaps.md) — auto-generated by `ingestion/verify.py`
3. **Import issues:** [`catalog/import-review.md`](catalog/import-review.md), [`catalog/post-mapping-review.jsonl`](catalog/post-mapping-review.jsonl) (latter auto-generated by `organize_posts_from_csv.py`)
4. **X raw cache:** [`import/x-posts-raw.csv`](import/x-posts-raw.csv) (gitignored) — sync first, organize second. Replies to other users are excluded from attribution.

## One episode

For `ep-NNNN` (4-digit padded id, e.g. `ep-0200`), use the same `{folder}` basename under each tree:

- `content/transcripts/{folder}/{folder}.transcript.md`
- `content/notes/{folder}/{folder}.notes.md` (raw datapoints)
- `content/notes/{folder}/{folder}.expanded.md` (optional, after datapoint workflow)
- `content/posts/{folder}/{folder}.post.md`

Resolve `{folder}` from catalog `id` + `slug` via `paths.folder_name` (see [`docs/episode-id-rules.md`](docs/episode-id-rules.md)).

## Cross-episode search

```bash
cd ingestion
python search.py "rockefeller"
python build_chunks.py   # refresh catalog/chunks.jsonl first
```

Or ripgrep: `content/transcripts/`, `content/notes/`, `content/posts/`, `content/posts/_corpus/all-posts.md`

## Datapoint expansion

User workflow: timestamp bullets in `{folder}.notes.md` → full quotes + takeaways. See [`docs/datapoint-workflow.md`](docs/datapoint-workflow.md).

```bash
python expand_datapoints.py --id ep-0200
```

## Ingestion (minimal)

Full script index: [`ingestion/README.md`](ingestion/README.md).

| Task | Command |
|------|---------|
| New episodes | `python sync_new.py --repair-urls --apply` → `map_colossus.py` → `fetch_transcripts.py` → `scaffold_notes.py --missing` |
| Notes scaffold | `python scaffold_notes.py --missing` or `--next` (see [`docs/notes-pipeline.md`](docs/notes-pipeline.md)) |
| Edit notes | Directly in `content/notes/{folder}/{folder}.notes.md` (Working Copy / Cursor) — no Apple Notes import |
| X cache sync | `python sync_x_cache.py` (incremental) or `--full` (backfill) |
| X organize | `python organize_posts_from_csv.py` (reads CSV only; skips native X articles) |
| X LLM match | `python attribute_posts_llm.py --dry-run` / `--apply` (ambiguous rows in `post-mapping-review.jsonl`) |
| Manual post | `python assign_post_manual.py --episode N --x-post-id ID --published-at YYYY-MM-DD --body-file path` |
| Dedupe CSV | `python dedupe_x_csv.py` (after overlapping `--full` syncs) |
| Chunk index | `python build_chunks.py` |
| Verify | `python verify.py` |

Do not add embeddings unless grep + chunk index fail for the user's post corpus. See [`docs/retrieval.md`](docs/retrieval.md).

## Schema

[`docs/episode-id-rules.md`](docs/episode-id-rules.md) — ids, frontmatter, catalog columns. Daily notes: [`docs/notes-pipeline.md`](docs/notes-pipeline.md).
