# Colossus login (free account at https://colossus.com/register/)
COLOSSUS_EMAIL=you@example.com
COLOSSUS_PASSWORD=your-password

# X API v2 (https://developer.x.com/) — sync_x_cache.py
X_BEARER_TOKEN=your-bearer-token
X_USERNAME=ethanfrost

# Optional if lookup-by-username is disabled:
# X_USER_ID=your-numeric-user-id
# Workflow: sync_x_cache.py → organize_posts_from_csv.py (see import/README.md)

# OpenRouter — expand_datapoints_llm.py, expand_tune.py, attribute_posts_llm.py
OPENROUTER_API_KEY=your-openrouter-api-key
# Any slug from https://openrouter.ai/models — swap when tuning cost/speed/quality (optional :nitro suffix)
OPENROUTER_MODEL=deepseek/deepseek-v4-pro
# Post attribution — attribute_posts_llm.py (optional; separate slug, or omit for script default)
OPENROUTER_ATTRIBUTION_MODEL=your-provider/your-attribution-model
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
