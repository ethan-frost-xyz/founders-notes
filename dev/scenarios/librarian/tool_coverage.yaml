name: Librarian tool coverage
llm: live
turns:
  - send: "What did I note about Rockefeller discipline across episodes?"
    expect:
      expect_live:
        tool_called: search_vault
  - send: "What did he say verbatim about Standard Oil breakup?"
    expect:
      expect_live:
        tools_called:
          - search_transcript
  - send: "Load episode ep-0016."
    expect:
      expect_live:
        tool_called: load_episode
  - send: "List episodes matching rockefeller."
    expect:
      expect_live:
        tool_called: list_episode_ids
